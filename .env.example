# auth-gateway configuration (copy to .env and adjust)

# The upstreams to route between (required): an ordered, comma-separated
# key=url list. Order is the button order on the selector page; the key is what
# the routing cookie stores, so it must stay stable once users have picked.
# Keys match [a-z0-9_-]{1,64}.
BACKENDS=corp=http://localhost:4180,guest=http://localhost:4181

# Optional button labels (key=label). Defaults to the key, first letter
# capitalized. No commas in a label.
# BACKEND_LABELS=corp=Corporate SSO,guest=Guest access

# Address the gateway listens on (default :8080).
LISTEN_ADDR=:8080

# Name of the routing cookie (default auth_mode).
COOKIE_NAME=auth_mode

# How many trips to the IdP one not-yet-proven choice may make (default 1).
# A second sign-in start under the same temporary cookie means the first came
# back without a session, so the user is sent to the selector instead of to the
# same IdP again. See README: "Recovering from a wrong choice" (mechanism 4).
# COOKIE_TEMP_MAX_STARTS=1

# Exact path that hands the user off to the IdP (optional). Unset, any auth path
# that answers with a 302 to another host counts as a sign-in start; set it to
# pin the detection to one path, e.g. /oauth2/start.
# AUTH_START_PATH=/oauth2/start

# Custom selector page (optional, mutually exclusive — setting both is fatal).
# Either is a *shell*: one self-contained HTML document containing
# <!--AUTH_GATEWAY_SELECTOR--> exactly once, where the gateway splices in the
# selector widget it owns. See README: "Custom selector page".
# SELECTOR_HTML_FILE=/etc/auth-gateway/selector.html
# SELECTOR_HTML=<!doctype html><html><body><!--AUTH_GATEWAY_SELECTOR--></body></html>
